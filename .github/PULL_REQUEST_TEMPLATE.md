<!--
The pull request title becomes the commit on main, so it must be a
conventional commit:

  type(optional scope): subject      e.g. fix(html): escape file names

Emojis are welcome after the colon. See CONTRIBUTING.md.
-->

## What does this change?

## Why?

<!-- Closes #123 -->

## How was it verified?

- [ ] `pnpm test` passes
- [ ] `pnpm lint:ci` and `pnpm typecheck` pass
- [ ] Ran the CLI against a real project, or added/updated a test
- [ ] If this touches packaging or the HTML reporter: `pnpm check:package` and `pnpm e2e` pass

## Notes for the reviewer

<!-- Breaking change? New dependency? Does the report output change? -->
